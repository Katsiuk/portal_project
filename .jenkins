pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                bash 'echo "Hello, World!'
                bash 'apt update && apt upgrade -y'
            }
        }
    }
}